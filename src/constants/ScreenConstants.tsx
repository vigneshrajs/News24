export type ScreenName = keyof undefined;

const HOME_SCREEN = 'HomeScreen' as ScreenName;
const AppNavigator = 'AppNavigator' as ScreenName;
const MAIN_SCREEN = 'MainScreen' as ScreenName

export const ScreensConstants = {
  HOME_SCREEN,
  AppNavigator,
  MAIN_SCREEN,
};
